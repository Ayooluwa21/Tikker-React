# Tikker - Server

Minimal Express + MongoDB backend example.

Quick start:

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```bash
npm install
```

3. Run in development with nodemon:

```bash
npm run dev
```

Health check: `GET /api/health`
